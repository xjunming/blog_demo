# blog_demo
用Flask框架写的简单博客，功能只有注册登录、发表说说

## To run this demo. 
如果你有虚拟环境，可以运行`virtualenv env`, 然后激活虚拟环境 `source env/bin/activate`.
* `pip install -r requirement.txt`, 安装相关的包.
* `python create_db.py`, 创建数据库.
* `python run.py`, 运行.
